    // const { email, password } = options.auth;
    // agent.auth(email, password);
    // // const merged = `${email}:${password}`;
    // // const base64 = Buffer.from(merged).toString('base64');
    // // agent.set('Autherization', `Basic ${base64}`);
 
